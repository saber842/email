name: Email Step Demo

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: 22f3001731@ds.study.iitm.ac.in
        run: echo "Hello, world!"
